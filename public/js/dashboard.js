(() => {
  const token=localStorage.getItem('dp_token'); if(!token){location.href='/login.html';return;}
  const $=id=>document.getElementById(id); const money=n=>Math.round(Number(n)||0).toLocaleString('en-US');
  const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  let products=[], sales=[], expenses=[], debts=[], currentRange={from:'',to:''}, lastReports=null;
  async function api(path,opts={}){const r=await fetch(path,{...opts,headers:{'Content-Type':'application/json',...(opts.headers||{}),Authorization:'Bearer '+token}});if(r.status===401){localStorage.removeItem('dp_token');location.href='/login.html';return null}const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Hitilafu ya server.');return d}
  async function guardRole(){const d=await api('/api/me');if(!d)return null;if(d.user.role==='saler'){location.href='/saler.html';return null}if(d.user.role==='super_admin'){location.href='/super-admin.html';return null}return d}
  function toast(msg){const t=$('toast');t.textContent=msg;t.style.display='block';clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.style.display='none',3200)}
  function dateISO(d){const x=new Date(d);return new Date(x.getTime()-x.getTimezoneOffset()*60000).toISOString().slice(0,10)}
  function rangeFor(period){const today=new Date();today.setHours(0,0,0,0);let from=new Date(today),to=new Date(today);if(period==='7d')from.setDate(from.getDate()-6);if(period==='30d')from.setDate(from.getDate()-29);if(period==='3m')from.setMonth(from.getMonth()-3);if(period==='6m')from.setMonth(from.getMonth()-6);if(period==='year')from.setFullYear(from.getFullYear()-1);return {from:dateISO(from),to:dateISO(to)}}
  function queryRange(){return currentRange.from&&currentRange.to?`?from=${currentRange.from}&to=${currentRange.to}`:''}
  async function loadMe(meData){const d=meData||await api('/api/me');if(!d)return;$('userName').textContent=d.user.full_name||'Owner';$('businessName').textContent=d.business?.name||'Daftari+';const initials=(d.user.full_name||'DP').split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase();$('avatar').textContent=initials;const roleLabel=d.user.role==='owner'?'Owner':(d.user.role==='manager'?'Manager':'Owner');$('tbPill').textContent=`${roleLabel} — ${d.user.email||d.business?.name||''}`;$('subTitle').textContent=new Date().toLocaleDateString('en-US',{weekday:'long',year:'numeric',month:'long',day:'numeric'});}
  async function loadProducts(){const d=await api('/api/products');if(!d)return;products=d.products||[];$('kpiProducts').textContent=products.length;$('kpiStock').textContent=money(products.reduce((a,p)=>a+p.quantity*p.buy_price,0));$('kpiLow').textContent=products.filter(p=>p.quantity<=p.min_stock).length;renderProducts();renderLow();renderInsights();renderCategoryDonut()}
  async function loadReports(){const d=await api('/api/reports'+queryRange());if(!d)return;lastReports=d;sales=d.sales||[];expenses=d.expenses||[];const s=d.summary||{};$('kpiSales').textContent=money(s.revenue);$('kpiProfit').textContent=money(s.profit);$('kpiExpenses').textContent=money(s.expenses);$('kpiMargin').textContent=`${Number(s.margin||0).toFixed(1)}%`;renderChart('chart',sales,expenses);renderSales();renderExpenses();renderInsights();$('rRevenue').textContent=money(s.revenue);$('rGross').textContent=money(s.grossProfit);$('rExpenses').textContent=money(s.expenses);$('rProfit').textContent=money(s.profit);$('reportPeriod').textContent=`${d.from} → ${d.to}`;renderChart('reportChart',sales,expenses);renderCategoryDonut();renderRecentTx()}
  async function loadDebts(){const d=await api('/api/debts');if(!d)return;debts=d.debts||[];const open=debts.filter(x=>x.status!=='voided'&&Number(x.amount)>Number(x.paid));$('kpiDebt').textContent=money(open.reduce((a,x)=>a+(Number(x.amount)-Number(x.paid)),0));$('debtTotal').textContent=`Outstanding: ${money(open.reduce((a,x)=>a+(Number(x.amount)-Number(x.paid)),0))}`;renderDebts()}

  const CAT_COLORS=['#F5B948','#0FA968','#3B82F6','#8B7CF6','#DC2626','#EC4899','#14B8A6','#F97316'];
  function renderCategoryDonut(){
    const el=$('categoryDonut'); if(!el) return;
    if(!products.length || !sales.length){ el.innerHTML='<div class="empty">Hakuna data ya kutosha bado.</div>'; return; }
    const catByProduct=new Map(products.map(p=>[p.id, p.category_name || 'Bila Category']));
    const totals=new Map();
    sales.forEach(s=>{
      const cat = catByProduct.get(s.product_id) || 'Bila Category';
      totals.set(cat, (totals.get(cat)||0) + Number(s.total||0));
    });
    const rows=[...totals.entries()].sort((a,b)=>b[1]-a[1]);
    const grand=rows.reduce((a,r)=>a+r[1],0) || 1;
    let acc=0, arcs='';
    const cx=100, cy=100, rOuter=90, rInner=58;
    rows.forEach((r,i)=>{
      const frac=r[1]/grand, start=acc, end=acc+frac; acc=end;
      const a0=start*2*Math.PI - Math.PI/2, a1=end*2*Math.PI - Math.PI/2;
      const large = (end-start) > 0.5 ? 1 : 0;
      const x0=cx+rOuter*Math.cos(a0), y0=cy+rOuter*Math.sin(a0);
      const x1=cx+rOuter*Math.cos(a1), y1=cy+rOuter*Math.sin(a1);
      const xi0=cx+rInner*Math.cos(a1), yi0=cy+rInner*Math.sin(a1);
      const xi1=cx+rInner*Math.cos(a0), yi1=cy+rInner*Math.sin(a0);
      const color=CAT_COLORS[i%CAT_COLORS.length];
      if (rows.length===1){
        arcs+=`<circle cx="${cx}" cy="${cy}" r="${(rOuter+rInner)/2}" fill="none" stroke="${color}" stroke-width="${rOuter-rInner}"/>`;
      } else {
        arcs+=`<path d="M${x0},${y0} A${rOuter},${rOuter} 0 ${large} 1 ${x1},${y1} L${xi0},${yi0} A${rInner},${rInner} 0 ${large} 0 ${xi1},${yi1} Z" fill="${color}"/>`;
      }
    });
    const legend=rows.map((r,i)=>`<div class="drow"><span class="dname"><i style="background:${CAT_COLORS[i%CAT_COLORS.length]}"></i>${esc(r[0])}</span><span class="dpct">${Math.round(r[1]/grand*100)}%</span></div>`).join('');
    el.innerHTML=`<div class="donut-wrap"><svg viewBox="0 0 200 200">${arcs}</svg><div class="donut-legend">${legend}</div></div>`;
  }

  function renderRecentTx(){
    const el=$('recentTxTable'); if(!el) return;
    const rows=[...sales].sort((a,b)=>new Date(b.created_at)-new Date(a.created_at)).slice(0,8);
    el.innerHTML = rows.length ? rows.map(s=>{
      const profit=(Number(s.sell_price)-Number(s.buy_price))*Number(s.quantity)-Number(s.discount||0);
      return `<tr><td>${esc(s.product_name)} ×${s.quantity}</td><td>${s.quantity}</td><td class="right mono">${money(s.total)}</td><td class="right mono goodtxt">+${money(profit)}</td></tr>`;
    }).join('') : '<tr><td colspan="4" class="empty">Bado hakuna mauzo.</td></tr>';
  }
  function renderProducts(){ $('productsTable').innerHTML=products.length?products.map(p=>`<tr><td><b>${esc(p.name)}</b></td><td>${esc(p.category_name||'—')}</td><td>${money(p.buy_price)}</td><td>${money(p.sell_price)}</td><td>${money(p.quantity)}</td><td>${money(p.quantity*p.buy_price)}</td><td>${p.quantity<=p.min_stock?'<span class="pill warn">Low</span>':'<span class="pill good">Healthy</span>'}</td><td><button class="btn" data-edit-product="${p.id}">Edit</button></td></tr>`).join(''):'<tr><td colspan="8" class="empty">Hakuna bidhaa.</td></tr>' }
  function renderLow(){const low=products.filter(p=>p.quantity<=p.min_stock).sort((a,b)=>a.quantity-b.quantity).slice(0,8);$('lowList').innerHTML=low.length?low.map(p=>`<div class="row"><div><div class="name">${esc(p.name)}</div><div class="meta">Minimum ${p.min_stock}</div></div><span class="pill ${p.quantity===0?'bad':'warn'}">${p.quantity===0?'OUT':p.quantity}</span></div>`).join(''):'<div class="empty">Stock iko vizuri 🎉</div>'}
  function renderInsights(){const sold=new Map();sales.forEach(s=>{sold.set(Number(s.product_id),(sold.get(Number(s.product_id))||0)+Number(s.quantity||0))});const best=products.map(p=>({...p,sold:sold.get(p.id)||0})).filter(p=>p.sold>0).sort((a,b)=>b.sold-a.sold).slice(0,6);const never=products.filter(p=>!sold.has(p.id)).slice(0,5);const slow=products.filter(p=>sold.has(p.id)).map(p=>({...p,sold:sold.get(p.id)})).sort((a,b)=>a.sold-b.sold).slice(0,5);const list=best.length?best.map((p,i)=>`<div class="row"><div><div class="name">${i+1}. ${esc(p.name)}</div><div class="meta">${p.sold} units</div></div><b>${money(p.sold*p.sell_price)}</b></div>`).join(''):'<div class="empty">Hakuna data ya mauzo.</div>';$('bestList').innerHTML=list;const arr=[...never.map(p=>({...p,label:'Never sold'})),...slow.map(p=>({...p,label:`${p.sold} sold`}))].slice(0,7);$('slowList').innerHTML=arr.length?arr.map(p=>`<div class="row"><div><div class="name">${esc(p.name)}</div><div class="meta">${p.label}</div></div><span class="pill ${p.label==='Never sold'?'bad':'warn'}">${p.label==='Never sold'?'Never':'Slow'}</span></div>`).join(''):'<div class="empty">Hakuna bidhaa.</div>'}
  function renderSales(){const rows=sales.slice().sort((a,b)=>new Date(b.created_at)-new Date(a.created_at)).slice(0,100);$('salesTable').innerHTML=rows.length?rows.map(s=>`<tr><td>#${s.id}</td><td>${esc(s.product_name)} ×${s.quantity}</td><td>${esc(s.customer_name||'Cash')}</td><td>${s.quantity}</td><td>${money(s.total)}</td><td>${esc(s.sold_by_name)}</td><td>${new Date(s.created_at).toLocaleString('sw-TZ',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</td><td><button class="btn danger" data-void-sale="${s.id}">Undo</button></td></tr>`).join(''):'<tr><td colspan="8" class="empty">Hakuna mauzo kwa kipindi hiki.</td></tr>'}
  function renderExpenses(){$('expensesTable').innerHTML=expenses.length?expenses.slice().reverse().slice(0,100).map(e=>`<tr><td>${esc(e.description)}</td><td>${esc(e.category||'—')}</td><td>${money(e.amount)}</td><td>${esc(e.created_by_name)}</td><td>${new Date(e.created_at).toLocaleString('sw-TZ',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</td></tr>`).join(''):'<tr><td colspan="5" class="empty">Hakuna matumizi kwa kipindi hiki.</td></tr>'}
  function renderDebts(){$('debtsTable').innerHTML=debts.length?debts.map(d=>{const bal=Number(d.amount)-Number(d.paid);return `<tr><td>${esc(d.customer_name||d.person_name)}</td><td>${esc(d.description||'—')}</td><td>${money(d.amount)}</td><td>${money(d.paid)}</td><td><b>${money(Math.max(0,bal))}</b></td><td>${esc(d.due_date||'—')}</td><td><span class="pill ${d.status==='paid'?'good':d.status==='voided'?'bad':'warn'}">${esc(d.status)}</span></td><td>${bal>0&&d.status!=='voided'?`<button class="btn" data-pay-debt="${d.id}">Pay</button>`:''}</td></tr>`}).join(''):'<tr><td colspan="8" class="empty">Hakuna madeni.</td></tr>'}
  function dayKey(s){return dateISO(s)}
  function renderChart(id,rows,exps){const el=$(id);if(!el)return;const map=new Map();rows.forEach(s=>{const k=dayKey(s.created_at);const v=map.get(k)||{sales:0,profit:0};v.sales+=Number(s.total||0);v.profit+=(Number(s.sell_price)-Number(s.buy_price))*Number(s.quantity)-Number(s.discount||0);map.set(k,v)});exps.forEach(e=>{const k=dayKey(e.created_at);const v=map.get(k)||{sales:0,profit:0};v.profit-=Number(e.amount||0);map.set(k,v)});let data=[...map.entries()].sort((a,b)=>a[0].localeCompare(b[0]));if(!data.length){el.innerHTML='<div class="empty">Hakuna data ya graph kwa kipindi hiki.</div>';return}if(data.length>31){const bucket=new Map();data.forEach(([k,v])=>{const d=new Date(k+'T00:00:00');const key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;const x=bucket.get(key)||{sales:0,profit:0};x.sales+=v.sales;x.profit+=v.profit;bucket.set(key,x)});data=[...bucket.entries()]}const w=900,h=250,pad={l:45,r:15,t:15,b:32},max=Math.max(1,...data.map(x=>Math.max(x[1].sales,Math.max(0,x[1].profit))));const x=i=>pad.l+(i/(Math.max(1,data.length-1)))*(w-pad.l-pad.r),y=v=>h-pad.b-(Math.max(0,v)/max)*(h-pad.t-pad.b);const salesPts=data.map((d,i)=>`${x(i)},${y(d[1].sales)}`).join(' '),profitPts=data.map((d,i)=>`${x(i)},${y(d[1].profit)}`).join(' ');const labels=data.map((d,i)=>{if(data.length>12&&i%Math.ceil(data.length/6)!==0)return '';return `<text x="${x(i)}" y="${h-8}" text-anchor="middle">${esc(d[0].slice(5))}</text>`}).join('');el.innerHTML=`<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><line x1="${pad.l}" y1="${h-pad.b}" x2="${w-pad.r}" y2="${h-pad.b}" stroke="#28304a"/><polyline class="line-sales" points="${salesPts}"/><polyline class="line-profit" points="${profitPts}"/>${data.map((d,i)=>`<circle class="dot-sales" cx="${x(i)}" cy="${y(d[1].sales)}" r="3"/><circle class="dot-profit" cx="${x(i)}" cy="${y(d[1].profit)}" r="3"/>`).join('')}${labels}</svg>`}
  async function refresh(){try{await Promise.all([loadProducts(),loadReports(),loadDebts()])}catch(e){toast(e.message)}}
  document.querySelectorAll('#nav button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('#nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.section').forEach(x=>x.classList.remove('active'));$(b.dataset.section).classList.add('active')}));
  document.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>$(b.dataset.open).classList.add('show')));document.addEventListener('click',e=>{const c=e.target.closest('[data-close]');if(c)$(c.dataset.close).classList.remove('show');const ep=e.target.closest('[data-edit-product]');if(ep)openEdit(Number(ep.dataset.editProduct));const vs=e.target.closest('[data-void-sale]');if(vs){$('voidSaleId').value=vs.dataset.voidSale;$('voidReason').value='';$('voidModal').classList.add('show')}const pd=e.target.closest('[data-pay-debt]');if(pd){const amt=prompt('Weka kiasi cha malipo:');if(amt)payDebt(Number(pd.dataset.payDebt),Number(amt))}});
  document.querySelectorAll('#globalFilters>button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('#globalFilters>button').forEach(x=>x.classList.remove('active'));b.classList.add('active');const p=b.dataset.period;$('customRange').style.display=p==='custom'?'flex':'none';if(p!=='custom'){currentRange=p==='day'?rangeFor('day'):rangeFor(p);refresh()}}));$('applyCustom').addEventListener('click',()=>{if($('fromDate').value&&$('toDate').value){currentRange={from:$('fromDate').value,to:$('toDate').value};refresh()}});
  $('productForm').addEventListener('submit',async e=>{e.preventDefault();try{const id=Number($('editProductId').value);const body={name:$('pName').value,buyPrice:Number($('pBuy').value),sellPrice:Number($('pSell').value),quantity:Number($('pQty').value),minStock:Number($('pMin').value)};await api(id?`/api/products/${id}`:'/api/products',{method:id?'PUT':'POST',body:JSON.stringify(body)});$('productModal').classList.remove('show');toast(id?'Bidhaa imehaririwa.':'Bidhaa imeongezwa.');await refresh()}catch(e){toast(e.message)}});
  function openEdit(id){const p=products.find(x=>x.id===id);if(!p)return;$('productModalTitle').textContent='Edit Bidhaa';$('editProductId').value=p.id;$('pName').value=p.name;$('pBuy').value=p.buy_price;$('pSell').value=p.sell_price;$('pQty').value=p.quantity;$('pMin').value=p.min_stock;$('productModal').classList.add('show')}
  document.querySelector('[data-open="productModal"]').addEventListener('click',()=>{if(!$('editProductId').value){$('productModalTitle').textContent='Ongeza Bidhaa';$('productForm').reset();$('editProductId').value='';$('pMin').value=5}});
  $('expenseForm').addEventListener('submit',async e=>{e.preventDefault();try{await api('/api/expenses',{method:'POST',body:JSON.stringify({description:$('eDesc').value,category:$('eCat').value,amount:Number($('eAmount').value)})});$('expenseModal').classList.remove('show');$('expenseForm').reset();toast('Matumizi yamehifadhiwa.');await refresh()}catch(e){toast(e.message)}});
  $('voidForm').addEventListener('submit',async e=>{e.preventDefault();try{await api(`/api/sales/${Number($('voidSaleId').value)}/void`,{method:'POST',body:JSON.stringify({reason:$('voidReason').value})});$('voidModal').classList.remove('show');toast('Sale ime-void na stock imerudishwa.');await refresh()}catch(e){toast(e.message)}});
  async function payDebt(id,amount){try{await api(`/api/debts/${id}/payments`,{method:'POST',body:JSON.stringify({amount})});toast('Malipo ya deni yamehifadhiwa.');await loadDebts()}catch(e){toast(e.message)}}
  $('logout').addEventListener('click',async()=>{try{await api('/api/logout')}catch{}localStorage.removeItem('dp_token');localStorage.removeItem('dp_user');location.href='/login.html'});
  currentRange=rangeFor('day');
  guardRole().then(meData=>{ if(!meData)return; loadMe(meData).then(refresh); });
})();

const T=localStorage.getItem('daftari_token'),U=JSON.parse(localStorage.getItem('daftari_user')||'null');if(!T||!U)location='/login.html';
const api=async(p,o={})=>{const r=await fetch(p,{...o,headers:{'Content-Type':'application/json',Authorization:'Bearer '+T}});const d=await r.json();if(!r.ok)throw Error(d.error);return d};const money=n=>'TSh '+Number(n||0).toLocaleString();
document.getElementById('who').textContent=U.full_name+' · '+U.role;logout.onclick=async()=>{await api('/api/logout',{method:'POST'});localStorage.clear();location='/login.html'};
async function render(p='sales'){const a=document.getElementById('app');if(p==='products'){let d=await api('/api/products');a.innerHTML='<div class="panel"><h2>Bidhaa</h2><form id="pf" class="filters"><input id="n" placeholder="Jina" required><input id="b" type="number" placeholder="Bei kununua" required><input id="s" type="number" placeholder="Bei kuuza" required><input id="q" type="number" placeholder="Stock" required><button>Ongeza</button></form><table class="table"><tr><th>Bidhaa</th><th>Buy</th><th>Sell</th><th>Stock</th></tr>'+d.products.map(x=>`<tr><td>${x.name}</td><td>${money(x.buy_price)}</td><td>${money(x.sell_price)}</td><td>${x.quantity}</td></tr>`).join('')+'</table></div>';pf.onsubmit=async e=>{e.preventDefault();await api('/api/products',{method:'POST',body:JSON.stringify({name:n.value,buyPrice:+b.value,sellPrice:+s.value,quantity:+q.value})});render('products');return}if(p==='reports'){a.innerHTML=`<div class="panel"><h2>Ripoti & Graph</h2><div class="filters"><select id="period"><option value="month">Mwezi</option><option value="week">Wiki</option><option value="3m">Miezi 3</option><option value="6m">Miezi 6</option><option value="year">Mwaka</option><option value="day">Siku</option></select><input id="year" placeholder="Mwaka, mfano 2026"><input id="from" type="date"><input id="to" type="date"><button id="go">Tafuta</button></div><div id="r"></div></div>`;go.onclick=loadReport;return loadReport()}if(p==='sales'){let d=await api('/api/sales');a.innerHTML='<div class="panel"><h2>Mauzo</h2><form id="sf" class="filters"><select id="prod"></select><input id="qty" type="number" value="1" min="1"><button>Uza</button></form><table class="table"><tr><th>Bidhaa</th><th>Qty</th><th>Jumla</th><th>Muuzaji</th><th>Tarehe</th></tr>'+d.sales.map(x=>`<tr><td>${x.product_name}</td><td>${x.quantity}</td><td>${money(x.total)}</td><td>${x.sold_by_name}</td><td>${x.created_at}</td></tr>`).join('')+'</table></div>';let ps=await api('/api/products');prod.innerHTML=ps.products.map(x=>`<option value="${x.id}">${x.name} — ${x.quantity}</option>`).join('');sf.onsubmit=async e=>{e.preventDefault();await api('/api/sales',{method:'POST',body:JSON.stringify({productId:+prod.value,quantity:+qty.value})});render('sales')};}else{let d=await api('/api/'+p);a.innerHTML='<div class="panel"><h2>'+p+'</h2><table class="table">'+Object.keys(d[p]||[]).length+'</table><pre>'+JSON.stringify(d[p],null,2)+'</pre></div>'}}
async function loadReport(){const z=new URLSearchParams();if(period.value)z.set('period',period.value);if(year.value)z.set('year',year.value);if(from.value)z.set('from',from.value);if(to.value)z.set('to',to.value);const d=await api('/api/reports?'+z);const s=d.summary;const max=Math.max(...d.sales.map(x=>x.total),1);r.innerHTML=`<div class="grid"><div class="stat"><small>Mauzo</small><strong>${money(s.revenue)}</strong></div><div class="stat"><small>Gross Profit</small><strong>${money(s.grossProfit)}</strong></div><div class="stat"><small>Matumizi</small><strong>${money(s.expenses)}</strong></div><div class="stat"><small>Faida Halisi</small><strong>${money(s.profit)}</strong></div><div class="stat"><small>Profit Margin</small><strong>${s.margin.toFixed(1)}%</strong></div></div><h3>Sales line</h3><div class="linechart">${d.sales.slice(-20).map(x=>`<div style="height:${Math.max(4,x.total/max*220)}px;width:${Math.max(3,90/d.sales.slice(-20).length)}%;display:inline-block;margin-right:4px;background:#00274d" title="${money(x.total)}"></div>`).join('')}</div>`}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>render(b.dataset.p));api('/api/dashboard').then(d=>{const s=d.summary;cards.innerHTML=Object.entries(s).filter(([k])=>['totalSales','totalRevenue','totalExpenses','totalDebts','totalProfit','productCount'].includes(k)).map(([k,v])=>`<div class="stat"><small>${k}</small><strong>${typeof v==='number'&&k!=='totalSales'&&k!=='productCount'?money(v):v}</strong></div>`).join('')});render();
